# Override the dev server's proxy target if cloudbroker isn't on localhost:8080.
# CLOUDBROKER_URL=http://localhost:8080

# Override the API base when the portal isn't same-origin with the API
# (production is same-origin, leave blank).
# VITE_API_BASE=
